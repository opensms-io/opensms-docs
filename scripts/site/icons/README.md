Icons vendored from the Iconsax library (`rounded/two-tone`), one file per icon, used
through the `icon()` helper in `../icons.mjs`. Directional affordances use only the
chevrons `arrow-down4`, `arrow-up3`, `arrow-left4` and `arrow-right4`.
